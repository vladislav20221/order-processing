package com.example.domain.enums;

public enum ErrorCode {
    AUTHENTICATION,
    UNAUTHORIZED,
    ACCESS_DENIED,
    ALREADY_EXISTS,
    INVALID_VALUE,
    NOT_FOUND,
    CONFLICT,
    INTERNAL_SERVER_ERROR,
    SERVER_ERROR,
    JWT_ERROR
}